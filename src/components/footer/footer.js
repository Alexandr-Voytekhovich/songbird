import React from 'react';

import './footer.scss'

import Button from '@material-ui/core/Button';

const Footer = () => {
  return (
    <footer>
      <Button variant="contained">Подтвердить догадку</Button>
    </footer>
  )
}

export default Footer;